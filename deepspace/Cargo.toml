[package]
name = "deepspace"
version = "0.2.0"
edition = "2021"
description = "DeepSpace — 太空物理引擎"
license = "MIT"

[features]
serde = ["dep:serde"]

[dependencies]
serde = { version = "1", features = ["derive"], optional = true }
